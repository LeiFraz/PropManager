type propertiesResponseData = {
  codigo_catastral: string;
  direc_calle: string;
  direc_piso: number | null;
  direc_numero: number | null;
  direc_puerta: string;
  dni_propietario: number | null;
};

export type {propertiesResponseData};
